[pytest]
pythonpath =
    src
testpaths =
    tests/azr_planner
markers =
    asyncio: mark a test as using asyncio
    compose: docker-compose integration test
addopts = -ra

[tool.pytest.ini_options]
addopts = "--cov=dgm_kernel"
